# The management / internal-backend admin user (admin/admin) is declared in rabbitmq/definitions.json,
# NOT here: once a loaded definitions file contains a users section, RabbitMQ ignores RABBITMQ_DEFAULT_USER
# and RABBITMQ_DEFAULT_PASS entirely. OAuth2 clients never use this user — they authenticate with their JWT.
